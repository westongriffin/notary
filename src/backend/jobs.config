{
  "jobs": [
    {
      "functionLocation": "/jobs/credentialExpiry.js",
      "functionName": "refreshCredentialStatus",
      "description": "Nightly: recompute credentialStatus on every profile so 'expiring'/'expired' stays accurate as time passes.",
      "executionConfig": {
        "cronExpression": "0 6 * * *"
      }
    }
  ]
}
