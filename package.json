{
  "name": "notary-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Record-keeping dashboard for notary.wes-griffin.com (Wix Velo, Git-integrated)",
  "scripts": {
    "postinstall": "wix sync-types",
    "dev": "wix dev",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@wix/cli": "^1",
    "@wix/eslint-plugin-cli": "^1",
    "eslint": "^8"
  }
}
